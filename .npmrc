# Configure Buf Schema Registry for @buf scoped packages
@buf:registry=https://buf.build/gen/npm/v1

# Buf token should be set in your environment or user-level .npmrc
# //buf.build/gen/npm/v1/:_authToken=${BUF_TOKEN}
